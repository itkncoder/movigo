import {memo} from "react"

const AdsHome = () => {
    return (
        <div>
            {/* Reklama className="p-3 ring-2 ring-gray-600 rounded-lg" */}
        </div>
    )
}

export default memo(AdsHome)